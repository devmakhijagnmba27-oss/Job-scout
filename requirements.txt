anthropic>=0.60
# <2 is load-bearing: mcp 2.0 removed mcp.server.fastmcp (renamed
# mcp.server.mcpserver), which mcp-server/job_search_server.py imports.
mcp[cli]>=1.9,<2
httpx>=0.27
pyyaml>=6.0
pypdf>=5.0
fpdf2>=2.7
rich>=13.0
python-dotenv>=1.0
pytest>=8.0
streamlit>=1.38
pandas>=2.0
google-genai>=0.1.0
openai>=1.0.0

# Optional, opt-in only (preferences.verify_liveness in profile.yaml):
# Playwright-based posting liveness verification. Not installed by
# default — most users won't need it, and it needs a separate browser
# binary download beyond pip install. To enable:
#   pip install playwright && playwright install chromium
# playwright>=1.40
