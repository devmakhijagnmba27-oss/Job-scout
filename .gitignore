# Secrets — NEVER commit credentials (rubric requirement)
.env

# Real user data — profile.example.yaml is the committed template
profile/profile.yaml
profile/resume.pdf
profile/*.pdf
profile/documents/*
!profile/documents/README.md

# Runtime artifacts
logs/
.jobscout_memory.json
.jobscout_records.json
output/

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# OS / editor
.DS_Store
.vscode/
.idea/

# Local agent tooling (machine-local settings + vendored skill copies)
.claude/
.agents/
.playwright-mcp/

